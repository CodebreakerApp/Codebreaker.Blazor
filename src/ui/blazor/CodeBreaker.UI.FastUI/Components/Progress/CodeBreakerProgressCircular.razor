@inherits CodeBreaker.UI.Shared.Components.Progress.CodeBreakerProgressCircularBase
@namespace CodeBreaker.UI

@if (Indeterminate)
{
    <FluentProgressRing></FluentProgressRing>
}
else
{
    <FluentProgressRing Min="Min" Max="Max" Value="Value"></FluentProgressRing>
}
